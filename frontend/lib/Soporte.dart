import 'package:flutter/material.dart';

class SoporteMegafonitoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Soporte Megafonito'),
      ),
      body: Center(
        child: Text('Aquí va la sección de soporte.'),
      ),
    );
  }
}
